{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-lsp",
      "type": "shell",
      "command": "cargo build -p restmd-lsp && npm run compile",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": ["$rustc", "$tsc"],
      "group": "build"
    }
  ]
}
